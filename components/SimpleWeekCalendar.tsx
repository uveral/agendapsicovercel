
'use client';

import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function SimpleWeekCalendar() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Simple Week Calendar</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Hello from SimpleWeekCalendar</p>
      </CardContent>
    </Card>
  );
}
